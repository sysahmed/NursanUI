# Конфигуриране на QR Код за Проследяване на Тикети

## Как да променя адреса за проследяване?

Адресът за проследяване на тикетите се конфигурира чрез XML файла `Baglanti.xml`.

### Стъпки за промяна:

1. **Отвори файла `Baglanti.xml`** в основната папка на приложението

2. **Намери секцията `ticketTracking`:**
```xml
<ticketTracking UrlTemplate="http://{serverIp}/tickets/track/{ticketId}">
</ticketTracking>
```

3. **Промени `UrlTemplate` атрибута** според нуждите си:

### Примери за различни адреси:

#### За локален сървър с порт:
```xml
<ticketTracking UrlTemplate="http://{serverIp}:8080/support/ticket/{ticketId}">
</ticketTracking>
```

#### За HTTPS адрес:
```xml
<ticketTracking UrlTemplate="https://{serverIp}/helpdesk/view/{ticketId}">
</ticketTracking>
```

#### За външен сайт:
```xml
<ticketTracking UrlTemplate="https://mycompany.com/tickets/track/{ticketId}?server={serverIp}">
</ticketTracking>
```

#### За custom път:
```xml
<ticketTracking UrlTemplate="http://{serverIp}/admin/tickets/details/{ticketId}">
</ticketTracking>
```

### Placeholders (заместители):

- `{serverIp}` - Автоматично се заменя с IP адреса на сървъра
- `{ticketId}` - Автоматично се заменя с ID-то на тикета

### Важни забележки:

- ⚠️ **Не премахвай** `{serverIp}` и `{ticketId}` от темплейта
- 🔄 **Рестартирай приложението** след промяна на конфигурацията
- 📝 Можеш да добавиш допълнителни параметри в URL-а
- 🔒 За HTTPS се увери че сървърът поддържа SSL

### Тестване:

След промяна на конфигурацията:
1. Запази XML файла
2. Създай нов тикет
3. Провери дали QR кодът съдържа новия адрес

### Възстановяване на default настройки:

Ако имаш проблем, върни default стойността:
```xml
<ticketTracking UrlTemplate="http://{serverIp}/tickets/track/{ticketId}">
</ticketTracking>
```

### Файлове за редактиране:

- **Основен файл:** `Baglanti.xml` (в главната папка)
- **Backup файл:** `OzelFolder/Baglanti.xml` (запазва същите настройки)
